# 핸드오프 — S9: 대검수 (명세·전체 문서 대비 전면 감사)

- 작성: S9 / 2026-08-28 (A~D 전 항목 수행 후 종료)
- 커밋: 없음 — `S9-fix:` 대상(한 줄짜리 명백한 버그)을 발견하지 못함. 워크트리 클린(`git status` 무변경, HEAD `59b34c0` = S8 병합 지점)
- ⚠️ 검수 기준 시점: 브랜치 `work/s9`(HEAD `59b34c0`). 통합 브랜치 `redesign/2026-08-28`에는 그 뒤 커밋 2건(`f7c8ca2` S5 재개 주의 문서, `0c6c05f` 메일 수신자 확정 — mail.ts·.env.example)이 더 있음. **본 검수는 이 2건을 포함하지 않는다.**

## 1. 완료한 것 — 검수 결과표

### A. 기계 검증 — 전부 PASS

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| A-1 | `npm run build` | **PASS** | 성공, 10 라우트, TS 통과, 오류 0 |
| A-2 | 금지어 확장 스윕 (납품·린넨·리넨·고온·업소용·취급하지 않·받지 않습니다·24시간·천북·054-621·배달·고정 시간·세탁일지·세 가지·네 단계·4단계) | **PASS** | `grep -rn` src/ 전체 **0건** (오탐 제외 불필요 — 히트 자체가 없음) |
| A-3 | 010-9828-3637 · 강동면 · 우수조달업체 존재 | **PASS** | tel `site.ts:65`, 강동면 `site.ts:69,70,78,79`, 우수조달업체 `site-footer.tsx:27`·`about/page.tsx:20,30` |
| A-4 | 삭제 라우트·파일 부재 (facility·faq·pricing·illustration·price-table) | **PASS(비고)** | 전부 부재 + 참조 grep 0건. `services/[slug]/page.tsx`만 `permanentRedirect("/services")` 스텁으로 존재 — S2/S6에서 확정된 처리 방식(RULES 6절 next.config 금지 하의 유일한 라우트 수준 redirect)이라 PASS 유지 |

### B. 명세 절별 전수 대조

**0~5절 회귀 검사 — 전부 PASS (S6 표와 동일 결과, 회귀 없음)**

| 절 | 결과 | 비고 |
|---|---|---|
| 0 전역 값 (tel·주소·tagline·description·nav 3항목·trustPoints·업종 6종) | **PASS** | `site.ts` 명세 표와 일치. 사우나→building·헬스장→office 임시 아이콘은 여전(미확정 이관 항목) |
| 1-1~1-9 홈 | **PASS** | 히어로 h1(D5 로테이션, 기본형 완성)·서브·절차 "첫 상담부터 배송까지"·4단계 명세 표 그대로·강점/시설/FAQ 부재·적합 업종 "확인 후 연락드립니다."+"바로 문의하기"·표준사업장 유지+번호 비표기·마무리 CTA — 전부 유지 |
| 2 회사소개 | **PASS** | PageHero 큰/작은 글씨, 개요 2단, 파트너 문구 페이지 끝 1회(`about/page.tsx:235~241`), 거래 대상 부정 없음 |
| 3 서비스 구조 | **PASS** | services[] 1건, itemGroups·reasons 없음, [slug] 308, forWhom 0절 일치 |
| 4 폼 5레이어 | **PASS** | schema.ts 7필드(company·contactName·phone·email·region·message·consent) 정확히, 폼 라벨 성함·주소, 빨간 `*`, 동의문 수집항목 "업체명 · 성함 · 연락처 · 주소 · 이메일" |
| 5 공통 UI | **PASS** | 헤더 "전화 문의", 푸터 시설·공정 없음·서비스 링크 1개·우수조달업체 문구 |

**8절**

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| 8-1 | 세탁일지 0건 | **PASS** | A-2 포함 |
| 8-2 | 빨간 배너 비표시 | **PASS** | `layout.tsx:96` 주석과 함께 미렌더. `sample-banner.tsx` 파일·스위치는 더미 관리용 유지(지시대로) |
| 8-3 | 지성이엔지 버튼 스타일 차용 (회사소개) | **FAIL(미반영)** | 어떤 세션 핸드오프에도 착수·추적 기록 없음. about 페이지 버튼은 공용 ButtonLink/ContactAction 그대로 — 차용 흔적 없음. **수정 제안: 담당 미지정 상태부터 해소 필요(오케스트레이터가 세션 배정 또는 회의에서 요구 철회 확인)** |
| 8-4 | 플로팅 위치·**상시 도크 형태** | **PASS** | `floating-contact.tsx` — 접힌 런처 없이 상시 노출 도크, PC 우측 중앙(`lg:top-1/2`)·모바일 우하단(CTA바 위). 커밋 `a9e6741` 사용자 확정 반영. ※ 명세 9-8 문면("펼치면")·8-4("오른쪽 아래")는 구본 — 아래 C-2 문서 갱신 목록 |
| 8-5 | forWhom 식당·월세탁 없음 | **PASS** | `services.ts:40` — 0절 6업종만 |
| 8-6 | 슬라이드 2+2 + 자동 넘김 | **PASS** | `sample.ts` slides: 확정 2장 + 지성이엔지 자리 2장(SAMPLE 게이팅), auto-slider 6초 자동·수동·reduced-motion 정지 |
| 8-7 | 흰 바탕 [미확정 유지 확인] | **PASS(대기 유지)** | 대상 미확인 상태 그대로 — 임의 확정 없음 (G6 대기) |
| 8-8 | 사우나 표기 [미확정 유지 확인] | **PASS(대기 유지)** | 칩 라벨 "사우나" 임시, `site.ts:101~103` 주석으로 추적 (G7 대기) |
| 8-9 | 지속 공유 | **N-A** | 진행 방식 항목 — 코드 검수 대상 아님 |

**9절**

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| 9-1 | 후기 마퀴 왼쪽 | **PASS** | `globals.css:258~260` `@keyframes jc-marquee { to { translateX(-50%) } }` + 카드 2회 렌더 |
| 9-2 | 관리자 인라인 편집 [미구현 예정 확인만] | **PASS(예정대로 미구현)** | `src/app/admin/` 하위 inquiries뿐. S5는 지시서만 갱신된 상태(커밋 e8c9ae6·60c0333), 구현 착수 전 — STATUS.md "보류(초안 이후)"와 일치. ※ 통합 브랜치의 `f7c8ca2`가 "S5 한도 중단 → 미커밋 진행분 계승"을 기록 — S5 재개 시 주의 |
| 9-3 | 전화 버튼 PC → /quote | **FAIL(1곳 잔존)** | 적용 확인: 헤더·홈 히어로/CTA·about CTA·services CTA(`661470c` 소급)·quote 연락 카드(DeviceSplit, PC는 번호만 표기)·푸터·플로팅. **잔존: `about/page.tsx:90` 사업자 정보 카드 대표전화가 `<a href={site.telHref}>` 직결** — PC에서 죽은 tel: 링크. S6 발견사항 ①의 "동일 패턴" 언급 지점인데 해소 커밋이 CTA 버튼 2곳만 고침. **수정 제안: ContactSplitLink 교체 1곳 — 오케스트레이터 직접 수정 가능 수준** (admin/inquiries 헤더 tel:은 관리자 내부 화면이라 S6 판단대로 제외) |
| 9-4 | 모바일 tel: 즉시 발신 | **PASS** | 전 지점 mobile 분기 tel:, mobile-cta-bar(lg:hidden) TelButton 유지 |
| 9-5 | SMS — **PC 비표시 확정** | **PASS** | SMS 진입점은 플로팅 도크의 문자 탭 1곳뿐(`floating-contact.tsx:114` `lg:hidden`). `ContactAction kind="sms"` 사용처 0곳 — PC에 SMS 계열 버튼 자체가 없음. mobile-cta-bar에 SMS 미추가는 S4 판단 보류 기록 유지 |
| 9-6 | SNS 버튼 | **PASS** | 푸터(`site-footer.tsx:39`) + quote 후기 섹션(`quote/page.tsx:159`), snsLinks null → "준비 중" 안내 |
| 9-7 | 더미 목록 주석 vs 실제 더미 전수 일치 | **PASS(비고)** | `sample.ts` 상단 주석 10종 = 실제 export 10종 일치(industryNotes 포함). 비고: **businessHours·certification·capacity·serviceAreas 4종은 사용처 0곳**(facility 삭제·운영시간 노출 제거·인증번호 비표기의 잔재) — 목록엔 남았으나 화면에 안 나옴. 오인 소지는 없으나 정리 후보 |
| 9-8 | 플로팅 5채널 | **PASS** | 전화(분기)·견적 폼·SMS(모바일 전용)·카카오톡·챗봇 5채널, 미개설 2종은 PendingDot+준비 중 안내+/quote 유도 |

**S8 신규 반영분**

| 항목 | 결과 | 근거 |
|---|---|---|
| 채택 시안 섹션 구성 일치 | **PASS** | PageHero → 핵심 카드(white, eyebrow "하나의 서비스, 사업장마다 다른 리듬"+summary 대제목+칩 6) → 운영 방식(tint "이렇게 운영합니다" 3카드) → 주간 리듬(navy, ScheduleCard 420px+글로우 16s/20s reverse) → 업종별(white "이런 사업장과 함께합니다" 6카드) → CTA(paper). 시안과의 의도적 차이 3건은 전부 지시서·핸드오프 근거 있음: ①운영 방식 카드 본문은 시안 문구가 아닌 기존 points 재사용(지시서 "문구 변경 없음") ②CTA는 시안의 center 정렬 대신 현행 유지(지시서) ③"[문구 확정 필요]" 화면 태그 미표기(핸드오프 5절 — sample.ts 게이팅으로 대체) |
| 업종별 문구 sample.ts 게이팅 | **PASS** | `industryNotes`(SAMPLE_CONTENT 게이팅, off면 칩만) — `services/page.tsx:150` 조건 렌더, 키는 targetIndustries label과 일치 |

### C. 문서 정합 감사 (코드 ↔ 문서) — **FAIL 다수 = 문서 쪽 최신화 필요**

**C-1. 가이드라인 2종 vs 코드**

| # | 어긋난 곳 | 상세 |
|---|---|---|
| ① | 페이지별-레시피 홈 구성 구본 | 레시피: 히어로→신뢰 스트립(white)→서비스(tint)→절차(white)→표준사업장→CTA. 실코드: 히어로(배지는 히어로 안)→**자동 슬라이드(white)**→서비스(**white**)→절차(**tint**)→**적합 업종(tint)**→표준사업장→CTA. 슬라이드·적합 업종 섹션이 레시피에 없고 톤 배치도 다름 |
| ② | 페이지별-레시피 /services 구본 | S8 신설 "업종별(white)" 섹션 누락 (navy와 paper 사이) |
| ③ | 페이지-구성-규칙 "같은 톤 섹션 연속 금지" vs 홈 실코드 | 슬라이드(white)→서비스(white), 절차(tint)→적합 업종(tint) 연속 — 규칙 위반 상태이거나 규칙이 실태와 불일치. **회의/오케스트레이터 판단 필요(코드를 고칠지 규칙을 완화할지)** |
| ④ | 구성-규칙·레시피에 플로팅 도크 기술 없음 | 상시 도크(확정 사항)가 가이드 어디에도 없음 — 디자인 에이전트가 다시 "런처" 형태를 만들 위험 |
| (참고) | 구성-규칙의 확정 문구·금지어·절차 4단계 | 코드와 **일치** — 어긋남 없음 |

**C-2. 명세 6절 "확인 대기" vs 현재 코드**

| # | 항목 | 판정 |
|---|---|---|
| #5 운영시간 | **명세 구본** — "견적 페이지에 임시값 노출 중(부분 블로킹)"이라 기술돼 있으나 S2가 노출부를 제거하고 폼 안내 문구로 대체(`quote/page.tsx:63`). 블로킹 해소됐는데 대기로 남음 → 명세 갱신 필요 |
| #2 사업자등록증 | **명세 내 경미한 모순** — 6절 #2 "임시값 배너 유지"는 8-2(상단 배너 제거)와 충돌. 실코드는 상단 배너 대신 about 카드 내 경고문+푸터 경고로 대체 — 취지는 충족, 문면 정리 필요 |
| #1·#3·#4·#6·#7·#8·#9 | **일치** — 전부 대기 상태 그대로 코드에 반영(인증번호 비표기/SMS·후기 더미/priceNote 노출 중/eyebrow 임시/카카오톡·챗봇 준비 중) |

**C-3. 요구사항 문서 G1~G10·기타 상태 최신화 필요 목록**

- G8 운영시간: "견적 페이지에 임시값 노출 중" → 노출 제거됨으로 갱신
- B7·B13·H8: 플로팅이 "클릭 시 견적 페이지 이동 단일 버튼"·"오른쪽 아래"·"런처 펼침"으로 기술 → 상시 5채널 도크·PC 우측 중앙으로 갱신 (명세 8-4·9-8 문면도 동일)
- H 매트릭스 "문의하기 PC → /quote 이동" → "PC 표시하지 않음"(2026-08-28 확정, 명세 매트릭스에는 반영됨)으로 통일
- G1·G2·G3·G4·G5·G6·G7·G9·G10: 대기 상태 그대로 — 변경 없음 확인
- **STATUS.md 대폭 구본**: S4 "대기"(실제 완료·병합 988d0bc), S7·S8·S9 행 자체가 없음, S5 지시서 갱신·재개 주의 미반영 → 재작성 필요
- **S8-handoff.md 미커밋**: 메인 레포에 untracked(`??`) 상태 — RULES 4절(핸드오프 없는 종료 = 실패) 취지상 커밋 필요. S5 핸드오프는 부재(세션 자체가 중단 상태라 정합)
- 구현-체크리스트: 역사적 기록 성격(“107곳” 등 착수 전 기준)이라 갱신 불요 판단 — 단 문서 머리에 "착수 전 기준" 명시가 있으면 좋음

**C-4. NOTES·컨벤션(.design-sync/) vs 코드**

- `conventions.md` "Icon 27종" → 실제 **30종**(chevronLeft·chat·sms·bot·instagram·blog 추가 후 표류). 재동기화 시 갱신 필요
- `NOTES.md`의 나머지 기술(QuoteForm 번들 제외·심링크·cssEntry 해시 등)은 빌드 구성 사실과 일치 — 어긋남 없음
- (경미) `mobile-cta-bar.tsx:7` 주석 "호텔 실장·**식당** 사장" — 식당은 대상 업종에서 빠짐. 기능 무관 주석이나 용어 정합상 정리 후보

### D. 실행 확인 — 전부 PASS

`npx next dev -p 3300` 기동 → curl 실측 → 종료(트리 클린 확인).

| 항목 | 결과 |
|---|---|
| `/` `/about` `/services` `/quote` | 전부 **200** |
| `/` 문구 3종 | "수거부터 배송까지" · "약속한 날짜에 수거하고, 세탁·살균을 거쳐 배송합니다" · "첫 상담부터 배송까지" 존재 |
| `/about` 문구 3종 | "(주)지성이엔지 지성크리닝" · "우수조달업체 …세탁 사업 부문입니다" · "(주)지성이엔지의 파트너 지성크리닝입니다" 존재 |
| `/services` 문구 | "사업장 규모, 품목과 물량에…" · "안전한 세탁·살균을 거쳐…" · "이렇게 운영합니다" + **"사업장의 한 주에" 존재** |
| `/quote` 문구 3종 | "확인 후 연락드립니다" · "성함" · 동의문 수집항목("업체명 · 성함 · 연락처 · 주소 · 이메일") 존재 |
| `/services/linen` | **308 → /services** |
| `/facility` | **404** |

## 2. 변경 파일 요약

없음 — 검수 전용. `S9-fix` 커밋 대상 미발견(유일한 코드 결함인 about:90 tel: 직결은 import 추가+컴포넌트 교체가 필요해 "한 줄짜리" 범위 밖).

## 3. 검증 결과

- `npm run build`: 성공 (A-1)
- 재현 명령: A-2 `grep -rn "납품\|린넨\|리넨\|고온\|업소용\|취급하지 않\|받지 않습니다\|24시간\|천북\|054-621\|배달\|고정 시간\|세탁일지\|세 가지\|네 단계\|4단계" src/` → 0건 / D는 `npx next dev -p 3300` 후 curl

## 4. 발견사항 요약 (FAIL 및 조치 필요 — 담당 제안)

| # | 항목 | 담당 제안 |
|---|---|---|
| 1 | **9-3 잔존 1곳**: `about/page.tsx:90` 대표전화 tel: 직결 (PC 죽은 링크) | 오케스트레이터 직접 수정 (ContactSplitLink 교체 1곳) |
| 2 | **8-3 버튼 차용 미반영**: 착수·추적 기록 자체가 없음 | 오케스트레이터 → 세션 배정 or 회의(요구 유효 여부 확인) |
| 3 | 가이드 2종 구본 (홈·서비스 구성, 도크 미기술) + "같은 톤 연속 금지" 규칙 vs 홈 실태 충돌 | 문서 갱신은 오케스트레이터 / 톤 규칙 충돌은 회의 또는 디자인 판단 |
| 4 | 명세 6절 #5(운영시간)·#2(배너) 문면 구본, 8-4·9-8 도크 확정 미반영 / 요구사항 G8·B7·B13·H8 구본 | 오케스트레이터 문서 일괄 갱신 |
| 5 | STATUS.md 구본 (S4~S9 상태) + S8-handoff.md 미커밋 | 오케스트레이터 |
| 6 | (정리 후보) 미사용 더미 4종(businessHours·certification·capacity·serviceAreas), conventions.md 아이콘 수 27→30, mobile-cta-bar 주석 "식당" | 후속 세션 or 재동기화 시 |

## 5. 질문 / 판단 보류

- 기존 미확정 항목 현황(전부 코드에 대기 상태로 정합 반영, 신규 없음): priceNote 노출(#6) / 사업자 임시값(#2, G2) / SMS 문구(G3) / 후기 문구(G4) / 슬라이드 원본 2장(G5) / 흰 바탕 대상(G6) / 사우나 표기·아이콘(G7, 0절 ⚠️) / SNS 링크(9-6) / 운영시간 실값(G8) / 카카오톡·챗봇(G9·G10)
- [slug] 스텁 존재는 S2·S6에서 규정 정합으로 확정된 방식 — 재론 불요 판단

## 6. 다음 세션에 주는 주의

- 이 검수는 `59b34c0` 기준 — 통합 브랜치의 `0c6c05f`(mail.ts 수신자)·`f7c8ca2` 이후분은 미검수. 다음 QA는 통합 브랜치 최신에서 mail.ts 회귀만 추가 확인하면 됨.
- S5 재개 시 `f7c8ca2` 기록(한도 중단 → 미커밋 진행분 계승) 필독.
- dev 실측은 포트 3300에서 했고 종료함. 커밋되지 않은 변경 없음. 병합·푸시 안 함.

---

**총평 (초안 품질 판정)**: 코드는 초안 공유 가능 상태다. 명세 0~9절의 확정 사항은 기계 검증·문구 실측·구조 확인 전 지점에서 유지·반영돼 있고(회귀 0), S8 서비스 페이지 개편도 채택 시안·지시서와 정확히 일치한다. 실질 코드 결함은 about 사업자 정보 카드의 전화 링크 1곳(9-3 매트릭스 잔존)뿐이며 한 줄 수준의 소급으로 끝난다. 반면 **문서군이 코드를 따라오지 못하고 있다** — 플로팅 도크·운영시간·홈/서비스 구성 등 확정·반영이 끝난 사항이 명세·요구사항·가이드·STATUS에 구본으로 남아 있어, 다음 세션이나 디자인 에이전트가 구본 문서를 기준으로 작업하면 역행이 생긴다. 코드 수정 1건보다 문서 일괄 최신화가 더 급한 상태다. 8-3(버튼 차용)만 유일하게 착수 기록조차 없는 미반영 요구로 남아 있어 회의 확인이 필요하다.
