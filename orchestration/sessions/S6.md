# S6 지시서 — 검증·QA: 명세 대조 및 실행 확인

너는 지성크리닝 웹사이트 개편의 S6 검증 세션이다. **코드를 수정하지 않는다** (발견만 한다). 유일한 예외: 아래 "경미한 수정" 조건.

## 반드시 먼저 읽을 것
1. `/home/jimin/Jiseong-Cleaning/orchestration/RULES.md`
2. `/home/jimin/지성크리닝-상세-요구사항-명세.md` — **전체**
3. `orchestration/handoffs/` 전부

작업 디렉토리: `/home/jimin/Jiseong-Cleaning/wt/s6/website` (통합 브랜치 기준 워크트리, 브랜치 `work/s6`)
핸드오프 저장 위치(메인 레포): `/home/jimin/Jiseong-Cleaning/orchestration/handoffs/S6-handoff.md`

## 검증 항목 (전부 수행, 결과를 핸드오프에 항목별 PASS/FAIL로)

### A. 기계 검증
1. `npm run build` 성공
2. 금지어 스윕: `grep -rn "납품\|린넨\|리넨\|고온\|업소용\|취급하지 않\|받지 않습니다\|24시간\|천북\|054-621\|고정 시간\|세탁일지" src/` → **0건**
3. 필수 값: `grep -rn "010-9828-3637\|강동면" src/lib/site.ts` → 존재
4. 삭제 확인: `src/app/facility`, `src/app/services/[slug]`, `src/lib/faq.ts`, `src/components/illustration.tsx`, `price-table`, `pricing` 부재 + 참조 0
5. sitemap 에 facility/[slug] 없음

### B. 명세 대조 (명세 절 번호 순서로 체크리스트 작성)
- 0절 표의 모든 값 / 1절 홈 섹션 구성(삭제 3종 + 표준사업장 유지 + 인증번호 없음) / 2절 회사소개 문구 구조(큰/작은 글씨, 마무리 파트너 문구 1회) / 3절 서비스 단일화 / 4절 폼 필드 최종안 + DB 컬럼 보존 / 5절 헤더·푸터 / 8절 누락 보완(라벨 제거, 슬라이드) / 9절 신규(플로팅 5채널, 마퀴 방향=왼쪽, 기기별 분기 매트릭스, SNS, 더미 목록)

### C. 실행 확인
- `npx next dev` 기동(포트 3100) 후 curl 로 `/`, `/about`, `/services`, `/quote` 200 응답 + 각 HTML에 핵심 문구 존재 확인:
  - `/`: "수거부터 배송까지 사업장 세탁물을 대신 관리해 드립니다"
  - `/about`: "우수조달업체", "파트너"
  - `/quote`: "확인 후 연락드립니다"
- 구 URL `/services/linen` 접근 시 리다이렉트/404 처리 상태 기록
- 확인 후 dev 서버 종료

### 경미한 수정 (허용되는 유일한 코드 변경)
오타·깨진 import 등 **한 줄짜리 명백한 버그**만 수정 가능. 수정 시 커밋 머리말 `S6-fix:` 로 분리하고 핸드오프에 사유 기재. 문구·구조 판단이 필요한 건 수정 금지 — FAIL 로 보고만.

## 종료 조건
- 핸드오프에 A/B/C 전 항목 PASS/FAIL 표 + FAIL 별 재현 방법·담당 세션 제안
- 커밋할 변경이 있으면 `S6-fix:` 커밋, 없으면 커밋 없이 종료 가능 (핸드오프는 필수)
