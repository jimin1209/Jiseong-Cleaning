# S8 지시서 — 서비스 페이지 개편: 채택된 Claude Design 시안 반영

너는 지성크리닝 웹사이트 개편의 S8 워커 세션이다.

## 반드시 먼저 읽을 것 (순서대로)
1. `/home/jimin/Jiseong-Cleaning/orchestration/RULES.md` — 공통 규정 전부 준수 (용어 규칙 포함)
2. 채택된 시안: `orchestration/designs/Service-시안.dc.html` — **이 시안의 섹션 구성·문구를 코드로 옮기는 것이 임무** (dc 문법은 무시하고 구성·스타일 의도만)
3. 현재 페이지: `src/app/services/page.tsx` + `src/lib/services.ts` + `src/lib/sample.ts`
4. `orchestration/handoffs/` 의 S2·S4·S7 핸드오프 (services 페이지 이력·ScheduleCard·모션 규칙)

작업 디렉토리: `/home/jimin/Jiseong-Cleaning/wt/s8/website` (전용 워크트리, 브랜치 `work/s8`)
핸드오프: `/home/jimin/Jiseong-Cleaning/orchestration/handoffs/S8-handoff.md` / 병합·푸시 금지, 커밋까지만.

## 작업 범위 — `src/app/services/page.tsx` 재구성 (시안 섹션 순서대로)

1. **PageHero** — 현행 유지 (문구 동일)
2. **핵심 카드(white)** — 현행 유지하되 시안처럼: eyebrow 한 줄 "하나의 서비스, 사업장마다 다른 리듬" + 제목 "안전한 세탁·살균을 거쳐 약속된 날짜에 수거, 배송해 드립니다." 크게 + 업종 Chip 6개(targetIndustries 사용). hover 리프트(기존 카드 스타일). priceNote Alert 는 현행 유지.
3. **운영 방식(tint)** — 제목 "이렇게 운영합니다"(가짓수 표현 금지). 카드 3개는 기존 services[0].points 데이터 재사용 (문구 변경 없음).
4. **신규: 주간 리듬(navy)** — SectionHead(tone="dark", align="center", eyebrow "주 간 리 듬", title "사업장의 한 주에 세탁의 박자를 맞춥니다") + `ScheduleCard` 를 중앙 max-w 420px 로 크게 + 배경 글로우(jc-glow 기존 keyframes 재사용). S7 홈 히어로의 글로우 패턴 참조.
5. **신규: 업종별(white)** — SectionHead "이런 사업장과 함께합니다" + 카드 6종 그리드(Chip+아이콘 + 한 줄 설명). 한 줄 문구는 **`lib/sample.ts` 에 `industryNotes` 더미로 추가** (SAMPLE_CONTENT 게이팅 + 상단 더미 목록 주석 갱신 — 회의 확정 대기):
   호텔 "객실 회전에 맞춘 주기" / 모텔 "매일 도는 객실 물량" / 펜션 "주말 성수기 이불·시트" / 사우나 "수건 대량 순환" / 헬스장 "운동 수건·운동복" / 단체시설 "기숙사·연수원 침구"
   Reveal 시차 등장(70ms 간격).
6. **마무리 CTA(paper)** — 현행 유지 ("확인 후 연락드립니다." + 견적/전화 버튼 — 전화는 반드시 ContactAction).

## 하지 말 것
- 다른 페이지·컴포넌트 수정 (services/page.tsx + sample.ts + 필요시 services.ts 최소 수정만)
- 새 문구 창작 (위에 명시된 것만) / 금지어 / TelButton·tel: 직결 (ContactAction 사용 — S4 핸드오프 6절)
- 패키지 추가

## 종료 조건
1. `npm run build` 성공
2. 금지어 스윕 0건 + `npx next dev -p 3200` 기동 후 `/services` 200 + "사업장의 한 주에" 문구 존재 curl 확인 후 종료
3. 커밋 `S8:` / 핸드오프 작성
